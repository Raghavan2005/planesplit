# Requirements Traceability Matrix — PS31 PlaneSplit

Every clause of the problem statement (`ps.md`) broken into atomic, testable requirements. No requirement is marked Done without a corresponding automated test and demo proof — per `CLAUDE.md` §2 and §35.

| ID | PS Requirement | Interpretation | Implementation | Test | Demo Proof | Status |
|----|----------------|-----------------|-----------------|------|------------|--------|
| R1 | "Pure software model of a small network... No programmable switch or physical networking hardware required." | No real network stack, no Mininet/hardware/OpenFlow switches — a self-contained in-process simulation. | `Router`, `Network`, `Packet` classes (ARCHITECTURE.md §2); topology of 3–5 nodes (BUILD_PLAN.md). | Unit test instantiates a network and confirms it runs with zero external network calls / no OS network APIs used. | Demo runs fully offline with no external dependencies. | Planned |
| R2 | "One component exposes intended forwarding rules" | Control-plane intent must be a distinct, authoritative source, not inferred from the data plane. | Control Plane Manager computes routes and pushes to every router's `RIB` (ARCHITECTURE.md §2B). | Unit test: after a route push, every router's RIB reflects the new route immediately. | N/A (internal correctness, not directly demoed). | Planned |
| R3 | "...another executes packet forwarding from an independently maintained rule set" | Data-plane forwarding must use a table that is structurally and causally independent from the control-plane intent — never read directly from it. | Each router's `FIB`, updated only via the Update Channel (ARCHITECTURE.md §2B2); router forwarding logic reads `FIB` only. | Unit test: forwarding a packet never touches `RIB`; static/code-level check that no forwarding path references `RIB`. | Demo shows RIB and FIB diverging and staying diverged until the channel actually delivers an update — proving they are not silently kept in sync. | Planned |
| R4 | "Inject delayed... updates" | Ability to apply a FIB update after an arbitrary configurable delay. | Update Channel delay mode (ARCHITECTURE.md §2B2). | TEST_PLAN.md Scenario 1. | DEMO.md Phase 2. | Planned |
| R5 | "Inject... lost... updates" | Ability to silently drop a FIB update so it never applies. | Update Channel drop mode. | TEST_PLAN.md Scenario 2. | DEMO.md Phase 3. | Planned |
| R6 | "Inject... partially applied updates" | Ability to apply an update to only a subset of affected routers, or apply it in corrupted form (e.g. wrong prefix length). | Update Channel corruption/partial-application mode. | TEST_PLAN.md Scenario 3. | DEMO.md Phase 3 (extendable to a dedicated partial-application phase). | Planned |
| R7 | "...while tolerating a declared short propagation window after legitimate changes" | A mismatch occurring within N seconds of a legitimate RIB change must NOT be flagged as a violation. | Verifier tracks last-legitimate-change timestamp per flow/destination and suppresses alerts inside the window (ARCHITECTURE.md §2D). | TEST_PLAN.md Scenario 1 — probe inside window must show "expected, suppressed," not an alert. Add explicit boundary-condition test: probe fired at exactly `window_end - ε` and `window_end + ε`. | DEMO.md Phase 2 — probes inside window print as tolerated, not as alerts. | Planned |
| R8 | "Determine when the observed forwarding behavior is no longer a valid implementation of the current control-plane intent" | After the grace window elapses, a persisting mismatch must be positively detected — not just logged as a diff. | Verifier's post-window comparison logic (ARCHITECTURE.md §2D). | TEST_PLAN.md Scenario 2 (post-window probe). | DEMO.md Phase 3. | Planned |
| R9 | "The system must identify the specific flow or destination affected" | An alert must name the exact destination/prefix and flow, not just "something changed." | Verifier alert payload includes destination prefix + flow identifier. | Assert alert message contains the exact prefix used in the test setup. | DEMO.md Phase 3 sample output: `Affected Destination: 10.1.1.0/24`. | Planned |
| R10 | "...verify the practical consequence with simulated packets, rather than merely diffing two configuration files" | The proof mechanism must be an actual traced packet through real FIB-based forwarding logic (showing where it really ends up), not a RIB-vs-FIB table string/dict diff. | Packet Prober traces both intended (RIB) and actual (FIB) hop-by-hop paths using real longest-prefix-match logic (ARCHITECTURE.md §2C). | Assert the verifier's evidence includes a full hop list for both paths, not just a boolean/diff. | DEMO.md Phase 3: prints `Intended Path: A -> B -> D` / `Actual Path: A -> C (Dropped)`. | Planned |
| R11 | "One normal delayed update that converges within the allowed window" | Required demo scenario: legitimate delay, resolves before window expires, no alert. | Update Channel delay < grace window. | TEST_PLAN.md Scenario 1. | DEMO.md Phase 2. | Planned |
| R12 | "...and one injected divergence that persists beyond it" | Required demo scenario: drop/corruption, still mismatched after window expires, alert fires. | Update Channel drop/corruption mode. | TEST_PLAN.md Scenario 2 (and 3). | DEMO.md Phase 3. | Planned |
| R13 | "...with repeatable packet probes proving the latter violates the intended path" | The same scenario, run again with the same seed/config, must reproduce the identical detection and evidence — no reliance on wall-clock flakiness. | Virtual/deterministic clock (not real wall-clock sleep) driving all timing (BUILD_PLAN.md, ARCHITECTURE.md). | Run Scenario 2 twice with the same fixed seed/config; assert identical output. | One-command demo re-run produces identical PASS/FAIL output both times. | Planned |

## Also tracked (not PS text, but required by CLAUDE.md process rules)

| ID | Requirement | Source | Status |
|----|-------------|--------|--------|
| Q1 | No hardcoded detection results; UI/CLI output must reflect actual computed state. | CLAUDE.md §8 | Planned — verify at code review time, not just at design time. |
| Q2 | Negative/edge cases tested: true negative, false positive attempt, boundary condition, duplicate/out-of-order probe, empty/malformed input. | CLAUDE.md §10 | Mostly closed — `TEST_PLAN.md` Scenario 4 (true negative), Scenario 5 (false-positive / concurrent flows), and Scenario 6 (route flapping / rapid repeated changes to the same flow) now added. Malformed/empty input still not covered — add once the Packet/probe data structures exist in code. |
| Q3 | One-command start and one-command deterministic reset. | CLAUDE.md §29–30 | Closed — `python -m planesplit.cli.demo --all` is the one-command start; there is no reset step because every run builds a fresh in-memory topology from scratch (no persisted state), and output is deterministic (proven by `tests/test_repeatability.py`). See `README.md`. |

## Open gaps to close before implementation starts

- `TEST_PLAN.md` still doesn't cover duplicate or out-of-order probes, or malformed/empty input handling (part of CLAUDE.md §10's full list) — reasonable to defer until the `Packet` and probe data structures actually exist in code, since the exact malformed-input shape depends on that schema.
- ~~No requirement yet defines the exact grace-window duration~~ — **closed**: `GRACE_WINDOW_SECONDS = 2.0` is now declared as a named constant in `ARCHITECTURE.md` §2D and `BUILD_PLAN.md` §0.

## Consistency pass (this revision)

A full reread of all docs surfaced and fixed: the Update Channel (fault injector, R4–R6) had no assigned owner in `BUILD_PLAN.md`'s agent split; `BUILD_PLAN.md` and `TASK_BREAKDOWN.md` described two different, conflicting repo layouts (`src/` flat files vs. modular `core/`/`faults/`/`verify/`); and two cross-references (`MILESTONES.md` → "BUILD_PLAN.md §3", `TASK_BREAKDOWN.md` → "BUILD_PLAN.md §13") pointed at sections that no longer existed after `BUILD_PLAN.md` was rewritten. All four are fixed — `BUILD_PLAN.md` now has one canonical repo layout, explicit fault-injector ownership (Agent 1), an explicit Agent↔Workstream mapping table, and both cross-references now point at real sections.
